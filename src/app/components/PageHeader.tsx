export const PageHeader = ({ title }: { title: string }) => (
  <div className="-mx-6 sm:-mx-8 -mt-12 px-6 sm:px-8 py-10 bg-sky-950 text-white mb-10">
    <h1 className="text-3xl font-bold tracking-tight">{title}</h1>
  </div>
);
